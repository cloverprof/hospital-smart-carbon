# 实现差距记录（IMPLEMENTATION_GAPS）

更新日期：2026-08-04。每条含：差距、原因、当前替代、恢复路径。

## 1. 3D 环绕模式：等待提供可导出的 GLB

- **差距**：未交付 Three.js 3D 夜景环绕方案，仅交付静态夜景方案（全部名牌/弹窗交互完整）。
- **原因**：附件 `夜景.zip`（558 MB）内容为 3ds Max 2020 场景源文件（`夜景.max` 约 100 MB）+ 18 个 V-Ray 专有 `.vrmesh` 网格 + 216 张贴图。构建环境为 macOS：
  - 3ds Max 与 V-Ray for 3ds Max 均无 macOS 版本，本机无任何可打开 `.max`/`.vrmesh` 的已授权软件；
  - `.max`/`.vrmesh` 为闭源专有格式，不存在可靠的合法开源转换器；
  - 因此无法执行"精简→导出 GLB→压缩→分级加载"的既定流程。
- **定性**：这是**资产转换工具缺失**，不是浏览器硬限制。按主规格 2.3.4 条处理：不伪造模型转换，不提交伪 3D。
- **当前替代**：仓库自带高清夜景渲染图 `public/dashboard/hospital-night-campus.webp`（1672×941）作为静态场景层；楼宇名牌/弹窗用独立 DOM 层实现，两个驾驶舱与空间视图共用同一套楼宇 ID 与锚点配置。
- **顶栏表现**：`3D 环绕` 开关以禁用态显示，tooltip 说明原因（按 10.2 条，未过可行性门槛不提供可开启开关）。
- **恢复路径**：在装有 3ds Max + V-Ray 的 Windows 环境中：删除高面数植被与不可见物体 → Bake/转标准材质 → 导出 glTF/GLB → gltfpack（Meshopt）+ KTX2 纹理压缩，目标 <50 MB 首屏 → 将文件放入 `public/scene/`，在 `SceneStage` 挂载 3D 层（sceneMode 架构已预留）→ 按 performance-report.md 的门槛实测帧率。

## 2. 原始资产不入库

- 原始 `夜景.zip`、`.max`、`.vrmesh`、贴图未放入仓库与交付包：第三方模型（archmodels、3d66 等）许可证未确认，按主规格 2.3.6 不得公开分发。交付包只含仓库既有的夜景渲染图。

## 3. 东院区数据

- 组织模型含东院区（对比院区），但楼宇/表计数据未接入，相关面板显示"演示占位：等待真实数据/资产/接口"。接入真实数据后在 `src/data/buildings.ts` 补充楼宇即可。

## 4. 导出格式

- CSV/XLSX 导出为真实生成（与当前筛选一致）；PDF/XML 为带"Demo 模拟"水印说明的文本占位（浏览器端无依赖生成正式 PDF 需新增库，未纳入本次范围）。

## 5. COZE 仓库

- 权限不可访问（主规格 2.2 已预判）。缺失的合规资料改为在线核验，结果见 `docs/compliance-sources.md`；未核验项全部降级为演示参数。

## 6. 30 分钟长时运行测试

- 执行情况见 `docs/performance-report.md`"长时运行"一节；如该节标注未完成，则以该记录为准，不宣称通过。
