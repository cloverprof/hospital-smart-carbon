# 测试结果摘要

执行日期：2026-08-04。全部在本机真实执行，未通过的项目已如实标注。

## 1. 命令行门槛

| 命令 | 结果 |
|---|---|
| `pnpm lint` | 0 错误 0 警告 |
| `pnpm test` | 4 个测试文件 · **25 项全部通过** |
| `pnpm build` | TypeScript 与 Vite 构建 0 错误 |

## 2. 单元测试覆盖（25 项）

**减排计算器**（6 项，`calculator.test.ts`）
常规输入与单位换算正确；零投资回收期为 0 而非 NaN；零节能率回收期为 null 而非负数/Infinity；运维成本超过毛节省时年净节省为负且不可回收；非法输入（负值/NaN/Infinity/超范围节能率）被钳制；折现率为 0 时 NPV 等于寿命净收益。

**状态机**（5 项，`machines.test.ts`）
告警允许 待处理→处理中→已解决；禁止 已解决→任何状态与跳级；工单完整闭环 接收→判断→指派→处理→复测→复核→关闭；复测不合格可退回处理、关闭为终态；项目阶段按序推进、运行为终态。

**数据引擎**（7 项，`timeseries.test.ts`）
同一输入永远返回同一数值；全院日电耗 = 各楼宇之和；月度 = 该月日度之和；楼宇 24 小时之和 = 当日用量（±0.5%）；全院日碳排(t) ≈ 楼宇日碳排(kg) 之和 ÷ 1000；异常链 AN-001 期间夜间电耗占比显著上升；预测覆盖到 2026-09-30 且衔接近 14 日均值（±25% 内）、全部为有限正数。

**持久化**（7 项，`persistence.test.ts`）
`nsKey` 生成带版本前缀；重置演示只删本命名空间 key、外部应用数据保留；绝不调用 `localStorage.clear()`；旧 schema 版本被丢弃而不抛错；同版本数据原样保留；内存回退实现满足 Storage 接口；读取不存在的 key 返回 null。

## 3. 跨页一致性自检（`scripts/consistency-check.mjs`）

**10/10 通过**（真实浏览器执行）

| 检查项 | 结果 |
|---|---|
| 今日综合碳排跨页一致 | 门户 146.6 = 后勤舱 146.6 = 空间视图 146.6 tCO₂e |
| 演示基准日 2026-08-04 出现在各主要页面 | 5/5 页面 |
| 工单 WO-2026-0712 在驾驶舱弹窗与工单页同时存在 | 是 |
| 各页面无 NaN / undefined / Infinity | 全部干净 |
| Demo 模拟数据标注常驻 | 是 |
| 驾驶舱无侧栏且顶栏导航完整 | 是 |
| 驾驶舱 1920×1080 无横向溢出 | 溢出 0px |
| 驾驶舱 1366×768 无横向溢出 | 溢出 0px |
| `prefers-reduced-motion` 降级生效 | animation-duration → 1e-05s |
| 全流程控制台无错误 | 0 条 |

## 4. 真实浏览器截图（`scripts/screenshots.mjs`）

23 张截图覆盖 6 种分辨率（1366×768 / 1440×900 / 1920×1080 / 2560×1080 / 3440×1440 / 3840×1080），全部页面 + 两个驾驶舱名牌弹窗。生成过程 **0 控制台错误**。文件在 `docs/screenshots/`，索引见同目录 `README.md`。

## 5. 30 分钟长时运行（`scripts/soak.mjs`）

平均 60 FPS / 最低 55 / 1% low 60（1784 次逐秒采样）；内存 9→15 MB 无持续增长；DOM 节点 273→301 稳定；0 次 WebGL context lost；0 条未捕获错误。原始数据 `docs/soak-result.json`，详见 `docs/performance-report.md` 第 6 节。

## 6. 三条演示主线实测

| 主线 | 关键验证点 | 结果 |
|---|---|---|
| A 院领导决策 | 计算器算出年减碳 110.9 t / 回收期 5.9 年 → 生成项目 PRJ-2026-13（携带 calcParams）→ 领导舱 ROI 气泡图与减排成本效率同步更新 | 通过 |
| B 后勤闭环 | 后勤舱名牌弹窗显示 AHU-03 故障 + 额外碳排 277 kg/日 + 工单 WO-2026-0712 → 工单处理中→复测流转并写入日志 → 刷新后保留 | 通过 |
| C 核算与合规 | 缺失数据（AN-008）阻止锁定 → 补录估算值弹窗（插补参考值 14,873 kWh + 审计说明）→ 重新试算 25,287.7 tCO₂e → 二次确认锁定批次 HSC-2026-02~2026-07-B01 | 通过 |

## 7. 部署与权限验证

| 项目 | 结果 |
|---|---|
| 根路径部署 | HTTP 200，正常渲染 |
| 子目录部署（`/sub/dir/`） | HTTP 200，无资源 404 |
| 子目录 + 深链接刷新 | 直接渲染目标页面，不白屏 |
| 外部网络依赖 | 无 CDN / 在线字体 / 外部 API |
| 登录页三入口 | 文字准确、蓝色下划线小号、无验证码；跳转与默认角色（院领导/后勤负责人/能源管理员）及权限开关默认值全部符合规格 |
| 四角色权限矩阵 | 科室负责人仅见 3 个模块；深链接越权访问显示无权限提示；`全部权限` 开关独立生效 |
| 重置演示隔离性 | 种入两个外部 localStorage key，重置后均完好保留 |

## 8. 未执行或未通过的项目

- **3D 环绕方案未交付**：资产转换工具链在 macOS 上缺失（非浏览器硬限制），因此 3D 相关的帧率/内存门槛**未测试**。理由与恢复路径见 `IMPLEMENTATION_GAPS.md` 与 `docs/performance-report.md` 第 1 节。
- **PDF / XML 导出为占位**：CSV / XLSX 为真实生成，PDF / XML 输出带"Demo 模拟"说明的占位文件。
- **东院区数据未接入**：相关面板显示演示占位。
- **未创建 Pull Request**：见 `README.md` 与交付说明；按主规格第 15 条走 ZIP 交付路径。
